# Public app configuration (safe to bundle in the mobile app).
# Copy to app.env and adjust for your environment.

# Backend API base URL (no trailing slash).
# Android emulator: http://10.0.2.2:3000
# iOS simulator / desktop: http://localhost:3000
# Physical device: http://YOUR_PC_LAN_IP:3000
# Production: https://api.your-domain.com
QURAN_BACKEND_URL=http://10.0.2.2:3000
